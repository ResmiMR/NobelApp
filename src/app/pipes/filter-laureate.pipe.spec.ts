import { FilterLaureatePipe } from './filter-laureate.pipe';

describe('FilterLaureatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLaureatePipe();
    expect(pipe).toBeTruthy();
  });
});
